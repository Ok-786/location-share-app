import React, { Component } from 'react';
import View from '../views/NewPlace';

export default class NewPlace extends Component {
    render() {
        return (
            <View/>
        )
    }
}
